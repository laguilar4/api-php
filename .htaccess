RewriteEngine On
RewriteBase /api-php/

# Si el archivo o carpeta existe, usa eso
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Si no existe, envía todo a index.php
RewriteRule ^ index.php [QSA,L]
